#!/bin/zsh

# TODO: upgrade using for loops

def userinput():
    
    if data == 'A':
     addWater()
    elif data == 'B':
     removeWater()
    elif data == 'C':
     displayWater()
    elif data == 'D':
     exit()
    else:
     print("No such command")

x = 5
print(type(x))

def full(self, state):
        global water
        global waterlevel
        for waterlevel in iter(waterlevel):
            if (1):
                return True
        for waterlevel in iter(waterlevel):
            if (0):
                return False
        if waterlevel == (0):
            self.state == False
        elif waterlevel == (1):
            self.state == True